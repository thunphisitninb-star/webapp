services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: fod_user
      POSTGRES_PASSWORD: fod_password
      POSTGRES_DB: fod_database
    ports:
      - "5433:5432" 
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: